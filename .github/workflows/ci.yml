name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpango-1.0-0 libcairo2 libgdk-pixbuf-2.0-0 libffi-dev shared-mime-info fonts-dejavu-core
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Run tests
        run: python -m pytest tests/ -v

      - name: Smoke test server
        run: |
          uvicorn src.main:app --host 127.0.0.1 --port 8080 &
          pid=$!
          sleep 2
          curl -sf http://127.0.0.1:8080/health
          curl -sf http://127.0.0.1:8080/ | grep -q "Print Me Maybe"
          kill "$pid"
